namespace INSS.Platform.Portal.Web.Components.Register;

public class ClientRegistration
{
    // This class allows the client application to find the views within the shared folder root e.g. Views\Shared.
    //
    // See Program.cs in the consuming application for usage.
    // e.g.
    //
    // IMvcBuilder mvcBuilder = builder.Services.AddControllersWithViews()
    //  .AddApplicationPart(typeof(ClientRegistration).Assembly)
    //  .AddRazorOptions(options =>
    //  {
    //    options.ViewLocationFormats.Add("/Views/Shared/{0}.cshtml");
    //  });
}
