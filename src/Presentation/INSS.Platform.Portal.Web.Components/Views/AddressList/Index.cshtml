@{
    ViewData["Title"] = "All Addresses";
}

@addTagHelper GovUk.Frontend.AspNetCore.TagHelpers.*, GovUk.Frontend.AspNetCore
@addTagHelper INSS.Platform.Portal.Web.Components.TagHelpers.*, INSS.Platform.Portal.Web.Components

@model INSS.Platform.Portal.Domain.AddressListModel

<govuk-back-link href="@Model.PreviousPageUrl" />

<form method="post">
    <h1>Address list here!</h1>

        <govuk-summary-list>
        
        @foreach (var address in Model.AddressList)
        {
            <govuk-summary-list-row>
                <govuk-summary-list-row-key>
                    @(address.Title)
                </govuk-summary-list-row-key>
                <govuk-summary-list-row-value>
                    @foreach (var propValue in address.GetValues())
                    {
                        <p class="govuk-body">@(propValue)</p>
                    }
                </govuk-summary-list-row-value>
                <govuk-summary-list-row-actions>
                    <govuk-summary-list-row-action href="@(address.PageUrl)" visually-hidden-text="@Model.Title">Change</govuk-summary-list-row-action>
                </govuk-summary-list-row-actions>
            </govuk-summary-list-row>    
        }
        
    </govuk-summary-list>

    <govuk-button type="submit">Submit and continue</govuk-button>

</form>
